import { RoleTranscriberPipe } from './role-transcriber.pipe';

describe('RoleTranscriberPipe', () => {
  it('create an instance', () => {
    const pipe = new RoleTranscriberPipe();
    expect(pipe).toBeTruthy();
  });
});
